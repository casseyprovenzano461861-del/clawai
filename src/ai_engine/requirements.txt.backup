# AI引擎服务依赖 - 简化版本（先确保服务能启动）
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
redis

# LLM API支持（简化）
openai>=1.0.0

# 基础数据处理
numpy

# 监控和日志
prometheus-client
structlog

# 注意：暂时移除torch、transformers等重型依赖，后续逐步添加